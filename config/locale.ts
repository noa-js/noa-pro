type LocalConfig = {
  label: string;
  value: string;
};

const localConfig: LocalConfig[] = [
  {
    label: 'English',
    value: 'en_US',
  },
  {
    label: '简体中文',
    value: 'zh_CN',
  },
];

export default localConfig;
